<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/windowBackground">

    <androidx.camera.view.PreviewView
        android:id="@+id/pairingPreview"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginBottom="160dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/pairingStatus" />

    <TextView
        android:id="@+id/pairingStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textColor="?android:attr/textColorPrimary"
        android:fontFamily="@font/public_sans"
        android:gravity="center"
        android:padding="16dp"
        android:layout_marginBottom="16dp"
        android:text="@string/pairing_status_scan"
        app:layout_constraintBottom_toTopOf="@id/pairingManualButton" />

    <TextView
        android:id="@+id/pairingManualButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textColor="?android:attr/textColorSecondary"
        android:fontFamily="@font/public_sans"
        android:padding="16dp"
        android:text="@string/pairing_enter_manually"
        android:layout_marginBottom="32dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
